there is another usage for the qr code in the app when you open the document in the app you can vide document details (text) document visualization (pdf or image) or QR - when scanned it redirects the user to uaeverify.ae website , the weakness of this apprach is the at the verificatio result does not have any refrence for the original document for example if a user scanns the qr code for eid the qr code will redirect to uae verify and and show a statement saying that this document was issued by icp and is valid , the problem is that unless the person verifying is having access to the user’s device and have a way that the account is matching the user identity the verification does not hold any weight since there is no refrence to the original document 
I am thinking of another practical usage for the qr code 
one idea that we need to explore is to enable document sharing peer to peer between users currently we have a similar functionality but on service provider level where the service provider shows the qr code on their channel and once scanned through uae pass a sharing request is generated for that account towards the service provider. We want to also explore the on the cournter sharing so for example if a user goes to a hospital rather than sharing their eid physically they can scan a qr code approve eid sharing and it will arrive to the SP now for this there will be two ways of doing it , using the app to do the scanning but then we need to solve the problem of how the data will flow from the app to the sp system , who will be loging in the uae pass account ? do we need to introduce an admin account type where the data will follow , currently to integrate the sp  a full fledge onboarding process takes place where they integrate their system with uae pass system 
for the peer to peer case one problem is how will the document will be shared with the reciepient , how will will it be shared , for how long, what are the use cases where this is usefull. On another not the use case for hospital onboarding or visist person verification is strong the same can be done for hotel check in verification 
one idea I have is the QR code is on the reception and I go scan , receive sharing request for EID , I approve and my data flows to their system, it can be as simple as placing the EID in a specific storage location that their system can fetch. 
